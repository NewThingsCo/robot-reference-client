{
  "name": "robot-reference-client",
  "version": "1.0.0",
  "description": "Reference client for BrickPi3 Socket.IO server",
  "main": "robot.js",
  "private": true,
  "scripts": {
    "start": "DEBUG=* node robot.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "New Things Co",
  "license": "MIT",
  "dependencies": {
    "socket.io": "2.0.4"
  }
}
